@(Html.DevExtreme().Diagram()
    .ID("diagramControl")
    .Width("100%")
    .Height(800)
    .Toolbox(c=>c.Visibility(DiagramPanelVisibility.Disabled))
    .MainToolbar(c=>c.Visible(false))
    .HistoryToolbar(c => c.Visible(false))
    .ViewToolbar(c => c.Visible(false))
    .SimpleView(true)
    .ShowGrid(false)
)